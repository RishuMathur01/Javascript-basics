1. Arrays — highest priority

## -> Find the largest and smallest element in an array.
<!-- Find the second largest element. -->
Remove duplicates from an array.
Find duplicate elements in an array.
Find the missing number from 1...N.
Find the intersection of two arrays.
Find the union of two arrays.
Move all zeros to the end.
Rotate an array by K positions.
Find a pair whose sum equals a target — Two Sum.
Find the maximum subarray sum — Kadane's Algorithm.
Sort an array without using .sort().
Merge two sorted arrays.
Flatten a nested array.
Example:

Input: [1, 2, 2, 3, 4, 4]
Output: [1, 2, 3, 4]

2. Strings — very common

Reverse a string.
Check whether a string is a palindrome.
Count the frequency of each character.
Find the first non-repeating character.
Find duplicate characters.
Check whether two strings are anagrams.
Find the longest word in a sentence.
Capitalize the first letter of every word.
Remove duplicate characters from a string.
Reverse words in a sentence.
Find the longest substring without repeating characters.
Example:

Input: "listen", "silent"
Output: true

3. Objects / Hash Maps

Count the frequency of elements in an array.
Group objects by a property.
Find an object by a specific property.
Convert an array into an object.
Find duplicate objects.
Deep clone an object.
Merge two objects.
Find keys with duplicate values.

4. Recursion

Factorial using recursion.
Fibonacci using recursion.
Reverse a string recursively.
Sum all numbers in a nested array.
Flatten a nested array recursively.
Calculate x^n.
Traverse a nested object/tree.

5. Numbers / basic algorithms

Check whether a number is prime.
Find all prime numbers up to N.
Check Armstrong number.
Check palindrome number.
Reverse an integer.
Find factorial.
Generate Fibonacci series.
Find GCD and LCM.
Swap two numbers without a temporary variable.
🧠 JavaScript-specific coding questions
These are especially important because interviewers often want to see whether you actually understand JavaScript rather than just algorithms.

Implement your own map().
Implement your own filter().
Implement your own reduce().
Implement forEach().
Implement Array.prototype.myMap.
Implement debounce().
Implement throttle().
Implement once().
Implement memoize().
Implement Promise.all().
Implement a simple Promise.
Implement setTimeout behavior using promises.
Deep clone an object.
Deep compare two objects.
Implement currying.
Implement function composition.
Implement an event emitter.
For example, you may be asked:

const nums = [1, 2, 3, 4, 5];

const result = nums.myMap(x => x * 2);

console.log(result);
// [2, 4, 6, 8, 10]

and asked to implement myMap() yourself.

⭐ If you have limited time
Do these 20 first:

Reverse a string
Palindrome
Anagram
Character frequency
First non-repeating character
Remove duplicates from array
Find duplicates
Second largest number
Missing number
Two Sum
Move zeros to end
Merge sorted arrays
Flatten nested array
Maximum subarray sum
Fibonacci
Factorial
Prime number
Implement map
Implement filter
Implement reduce
For a typical frontend JavaScript interview, #18–20 plus debounce, throttle, Promise.all, closures, and async/await are particularly important.

If you're targeting 0–2 years experience, I can also give you a 50-question JavaScript coding-round sheet with solutions, from easy → medium → hard, which is a good practice sequence.